import React from "react";
import { Button, Block } from "components";

const Auth = () => {
  return (
    <div>
      <Block>
        <Button type="primary" size="large">
          Click
        </Button>
      </Block>
    </div>
  );
};

export default Auth;
