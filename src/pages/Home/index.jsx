import React from "react";
import { Route } from "react-router-dom";
import "./Home.scss";

const Home = () => (
  <section className="home">
    <h1>Home component</h1>
  </section>
);

export default Home;
